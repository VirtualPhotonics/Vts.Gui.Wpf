<UserControl x:Class="Vts.Gui.Wpf.View.Controls.RadioButtonOptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:designViewModels="clr-namespace:Vts.Gui.Wpf.DesignViewModels"
             d:Background="White"
             d:DataContext="{d:DesignInstance Type=designViewModels:ForwardAnalysisOptionViewModel, IsDesignTimeCreatable=True}">

    <HeaderedItemsControl ItemsSource="{Binding Options.Values}"
                          ItemTemplateSelector="{StaticResource OptionTemplateSelector}">
        <HeaderedItemsControl.Template>
            <ControlTemplate TargetType="{x:Type HeaderedItemsControl}">
                <StackPanel Orientation="Vertical">
                    <ContentPresenter ContentSource="Header" />
                    <ItemsPresenter />
                </StackPanel>
            </ControlTemplate>
        </HeaderedItemsControl.Template>
        <HeaderedItemsControl.Header>
            <TextBlock Text="{Binding GroupName, Mode=TwoWay}"
                       Visibility="{Binding ShowTitle, Converter={StaticResource MyBooleanToVisibilityConverter}}"
                       FontWeight="Bold" />
        </HeaderedItemsControl.Header>
        <HeaderedItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel VerticalAlignment="Center" />
            </ItemsPanelTemplate>
        </HeaderedItemsControl.ItemsPanel>
    </HeaderedItemsControl>
</UserControl>