<UserControl x:Class="Vts.Gui.Wpf.View.MieScatteringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="200" Height="190">
    <Grid Background="White">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="{Binding Path=MainResource.Label_MieScatteringLaw, Source={StaticResource LocalizedStrings}}" />
            <TextBlock Text="{Binding Path=MainResource.Label_MieNRel, Source={StaticResource LocalizedStrings}}" />
            <TextBlock Text=" = " />
            <TextBox Text="{Binding n, Mode=TwoWay}" Width="50" KeyDown="TextBox_KeyDown" />
            <TextBlock Text="{Binding Path=MainResource.Label_ScatteringRadius, Source={StaticResource LocalizedStrings}}" />
            <TextBlock Text=" = " />
            <TextBox Text="{Binding radius, Mode=TwoWay}" Width="50" KeyDown="TextBox_KeyDown" />
            <TextBlock Text="λ = " />
            <TextBox Text="{Binding wavelength, Mode=TwoWay}" Width="50" KeyDown="TextBox_KeyDown" />
        </StackPanel>
    </Grid>
</UserControl>